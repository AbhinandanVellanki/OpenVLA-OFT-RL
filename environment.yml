name: oft_rl
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - pillow>=9.5.0
  - numpy>=1.23.0
  - pip
  - pip:
    - transformers>=4.30.0
    - pytest>=7.4.0
    # Optional: uncomment if using flash attention
    # flash-attn>=2.0.0
